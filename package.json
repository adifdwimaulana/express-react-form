{
	"name": "ex-react-monorepo",
	"version": "1.0.0",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev:web": "pnpm --filter web dev",
		"dev:api": "pnpm --filter api dev",
		"dev": "concurrently \"pnpm dev:api\" \"pnpm dev:web\"",
		"build": "pnpm -r build"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"packageManager": "pnpm@10.17.1",
	"devDependencies": {
		"concurrently": "^9.2.1"
	}
}
